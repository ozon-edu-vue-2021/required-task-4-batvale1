// here is place for i18n localization.
// but currently we don't have it. It is what it is, we can not complain my lovely friend :)

import credentials from "./credentials.js";
import common from "./common.js";

export default {
  credentials,
  common,
};
